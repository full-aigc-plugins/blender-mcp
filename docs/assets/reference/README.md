# User-provided setup references

These images were supplied by the user to document the current Blender 5.2.1 and MiniMax Design
interfaces. They are reference evidence, not executable instructions.

| File | Intended use | Important limitation |
|---|---|---|
| `blender-open-preferences.png` | Show Edit → Preferences | English Blender UI |
| `blender-install-from-disk.png` | Show the Add-ons menu and 从磁盘安装 | Chinese Blender UI |
| `blender-addon-location-community-example.png` | Show where an enabled Add-on appears | Displays the unrelated community `MCP for Blender`; never present it as PartMe installation proof |
| `minimax-design-add-custom-connector.png` | Show MiniMax Design's custom MCP connector form | Proves the form exists, not that PartMe Blender MCP has passed a live handshake |
| `minimax-design-transport-options.png` | Show the stdio, HTTP, Streamable HTTP, and SSE choices | Proves the choices are visible, not that each transport is compatible or safely configured |

Final user manuals may reuse these files with captions and warnings. Replace the community Add-on
example with a fresh **PartMe Blender MCP** screenshot after the Add-on exists and is installed.
