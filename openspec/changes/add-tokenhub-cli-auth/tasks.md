## 1. Contract

- [x] 1.1 Add failing tests for CLI discovery, exit-code authorization, sanitized status and background login.
- [x] 1.2 Add failing UI/registry tests for the independent auth choice and honest non-executable state.

## 2. Implementation

- [x] 2.1 Implement the TokenHub CLI adapter without reading credential files.
- [x] 2.2 Add Blender preferences, configuration UI and browser authorization operator.
- [x] 2.3 Integrate status refresh without routing paid AI3D calls through undocumented interfaces.
- [x] 2.4 Add the documented TokenHub Bearer API Key adapter with bounded submit/query calls.
- [x] 2.5 Route professional/rapid generation through the shared transport and register all documented 3D model capabilities.
- [x] 2.6 Keep THCLI OAuth as a separate key-management helper rather than an execution credential.

## 3. Verification

- [x] 3.1 Run targeted and complete regression tests, compile checks and strict OpenSpec validation.
- [x] 3.2 Package/install the Add-on and verify the missing-CLI and configured UI states in real Blender.
- [x] 3.3 Package/install the revised Add-on and verify API-key configuration in real Blender.
- [ ] 3.4 Run a live paid generation only after the user configures a key and approves the cost.
